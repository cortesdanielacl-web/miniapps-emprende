/**
 * Rutas canónicas de la aplicación (modo producción).
 */

/** Entrada oficial a la calculadora. */
export const CALCULATOR_ENTRY_HREF = "/calculadora" as const

/** Inicio de checkout comercial (Webpay Plus). */
export const COMMERCIAL_CHECKOUT_HREF = "/compra/pagar" as const

/** Formulario para definir nueva contraseña tras el email de recovery. */
export const RESET_PASSWORD_HREF = "/reset-password" as const
