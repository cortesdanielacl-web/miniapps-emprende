/**
 * El documento PDF se genera con el motor compartido:
 *   import { createPdfReport, PdfDocument } from "@/components/pdf"
 *
 * Este archivo se mantiene como punto de entrada histórico del feature.
 */

export {
  createPdfReport,
  PdfDocument,
  type PdfReportDefinition,
} from "@/components/pdf"
