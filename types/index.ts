/**
 * Shared TypeScript types for MiniApps Emprende starter kit.
 * Extend this module per miniapp — keep domain types in features/.
 */

export type Nullable<T> = T | null

export type Optional<T> = T | undefined

export type StatusTone = "default" | "success" | "warning" | "info"
